# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - hotel-booking-engine

on:
  push:
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://1a4devcontain.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_fb08a423f1b745eaa2eee7aad273a3c4 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_a2142f6363954571bc75bd21d3bdfe54 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: 1a4devcontain.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_fb08a423f1b745eaa2eee7aad273a3c4 }}/amadeushotelbookingdjango:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'hotel-booking-engine'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_47cad4347c574369a22431166ac28e95 }}
        images: '1a4devcontain.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_fb08a423f1b745eaa2eee7aad273a3c4 }}/amadeushotelbookingdjango:${{ github.sha }}'